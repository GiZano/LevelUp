name: Build Android APK

on:
  push:
    tags:
      - 'v*' # Triggera la build quando crei un tag (es. v1.0.0)
  workflow_dispatch: # Permette di far partire la build manualmente da GitHub

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🛎️ Checkout repository
        uses: actions/checkout@v4

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: ☕ Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: 📦 Install Dependencies
        run: npm ci

      - name: 🏗️ Generate Native Android Project (Expo Prebuild)
        run: npx expo prebuild --platform android --clean

      - name: 🔨 Build APK
        run: cd android && ./gradlew assembleRelease

      - name: 📂 Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: LevelUp-APK
          path: android/app/build/outputs/apk/release/app-release.apk

      - name: 🚀 Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: android/app/build/outputs/apk/release/app-release.apk
          name: LevelUp ${{ github.ref_name }}
          generate_release_notes: true
